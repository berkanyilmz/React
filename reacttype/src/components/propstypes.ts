export type instructorProps = {
    instructorsNameLastName: {
        firstName: string,
        lastName: string,
    };
};

export type DataComing = {
    name: string;
    age: number;
    isUser: boolean;
};